class NameType {
    Zh_tw?: string; // 中文繁體名稱
    En?: string; // 英文名稱
}

class PointType {
    PositionLat?: number; // 位置緯度(WGS84)
    PositionLon?: number; // 位置經度(WGS84)
}

export { NameType, PointType };
